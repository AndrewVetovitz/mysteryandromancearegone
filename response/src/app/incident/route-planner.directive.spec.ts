import { RoutePlannerDirective } from './route-planner.directive';

describe('RoutePlannerDirective', () => {
  it('should create an instance', () => {
    const directive = new RoutePlannerDirective();
    expect(directive).toBeTruthy();
  });
});
